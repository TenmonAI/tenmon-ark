# CHAT_TRUNK_INFRA_WRAPPER_SPLIT_V1

第1段: `infra_wrapper_trunk_v1.ts` の `attachThreadCoreMirrorToResV1`（`__TENMON_THREAD_CORE` 付与の共通化用）。

continuity trunk は現状コールバック `setResThreadCoreMirror` を使用。全面置換は次イテレーション。
