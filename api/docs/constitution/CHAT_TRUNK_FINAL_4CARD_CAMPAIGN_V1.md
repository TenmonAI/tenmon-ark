# CHAT_TRUNK_FINAL_4CARD_CAMPAIGN_V1

## 完了スコープ（4カード）

1. **CHAT_TRUNK_SUPPORT_SELFDIAG_SPLIT_V1_FINAL** — `support_selfdiag_trunk_v1.ts` に consciousness / self-reflection / iroha counsel / self-diagnosis / system diagnosis residual / selfaware regex / shrink 系 SYSTEM_DIAGNOSIS を集約。`chat.ts` は dispatch のみ。
2. **CHAT_TRUNK_GENERAL_SPLIT_V1_FINAL** — `general_trunk_v1.ts` に `R22C_CONVERSATIONAL_PREEMPT`（`tryConversationalGeneralPreemptExitV1`）を移設。既存 `applyGeneralKuGroundingScriptureBumpV1` と併用。
3. **CHAT_TRUNK_INFRA_WRAPPER_SPLIT_V1_FINAL** — `infra_wrapper_trunk_v1.ts` の `attachThreadCoreMirrorToResV1` を shrink system diagnosis 経路の `setResThreadCore` に接続。
4. **CHAT_TRUNK_RUNTIME_ACCEPTANCE_LOCK_V1_FINAL** — 本憲章で主線4カードの acceptance 記録を固定。検証は `chatts_*` / `npm run build` / `execution_gate` / `replay_audit` / `full_autopilot` / `supervisor` を参照。

## PASS（運用）

- `chat_parts/**` 未編集
- `decisionFrame.ku` は既存 gate 経路で object 維持
- 単一スレッド作業（single_flight 前提）
