// 言靈・古事記系文献から「定義」「変換」「法則」を抜くための正規表現

/**
 * 定義抽出パターン
 */
export const DEFINITION_PATTERNS = [
  /(.+?)とは、(.+?)である/g,
  /(.+?)とは、(.+?)なり/g,
  /(.+?)を(.+?)という/g,
];

/**
 * 変換規則抽出パターン
 */
export const RULE_PATTERNS = [
  /(.+?)すると(.+?)となる/g,
  /(.+?)により(.+?)が生じる/g,
  /(.+?)は(.+?)を生む/g,
];

/**
 * 体系規則抽出パターン
 */
export const LAW_PATTERNS = [
  /火は(.+?)、水は(.+?)である/g,
  /(.+?)は天を、(.+?)は地を宰る/g,
  /五十音は(.+?)の構造である/g,
  /天地(.+?)人(.+?)同一/g,
];

/**
 * 演算子キーワード：これにより水火の数値を変動させる
 * 
 * 重要: fire（外発）とwater（内集）は対等なエネルギーである
 */
export const OPERATOR_KEYWORDS = {
  FIRE: ["発", "動", "開", "昇", "外", "行", "進", "陽", "火"],
  WATER: ["受", "静", "閉", "降", "内", "考", "止", "陰", "水"],
  CENTER: ["凝", "中", "正", "井", "核", "芯", "源", "空"],
};

